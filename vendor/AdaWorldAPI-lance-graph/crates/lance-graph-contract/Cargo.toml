[package]
name = "lance-graph-contract"
version = "0.1.0"
edition = "2021"
description = "VENDOR PARTIAL MIRROR — only the codegen_spine module of upstream AdaWorldAPI/lance-graph::lance-graph-contract is mirrored here. The full upstream crate has ~90 other modules (orchestration, NARS, cognition, recipes, …) that are not consumed by the OpenProject codegen surface in this repo. See ../README.md for design rationale."
license = "Apache-2.0"

[lib]
name = "lance_graph_contract"

[dependencies]
# Zero runtime deps — matches upstream's design intent.

# No [lints] block — vendor is a parsing/build aid, not a lint enforcement
# target. Upstream's full clippy::pedantic policy is not mirrored here.
