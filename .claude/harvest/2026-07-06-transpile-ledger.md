# OP → op-rs transpile LEDGER (V3 OGAR)

> The reproducible accounting of the OpenProject → op-rs transpile on the V3
> OGAR path (SurrealQL deprecated). Measured 2026-07-06 against corpus
> `/home/user/openproject` (`46c1fda2`), pins OGAR `2e346ea5` · ruff `9ef26c1b`
> · lance-graph `b05cc30b`.

## Provenance — the chain is ruff → OGAR, nothing hand-rolled
```
ruff_ruby_spo::extract_app_with_schema   (ruff harvest)
  → filter_to_core (op-nexgen curated 18)
  → ogar_from_ruff::mint::compile_graph_ruby::<OpenProjectPort>   (OGAR lift+mint → CompiledClass{class, facet, actions})
  → ogar_render_askama::render_class_with_methods(&class, FieldMask, &actions)   (OGAR render → Rust)
  → committed crates/op-generated/src/generated/<snake>.rs (+ `// @generated by ogar_render_askama` header)
```
Reproduce: `RAILS_CORPUS_SRC=/home/user/openproject cargo run -p op-codegen-pipeline --features ogar-emit --bin emit_generated`

## Counts

| metric | value | note |
|---|---|---|
| models extracted (pre-filter) | **945** | full tree (app/models + modules/*), not just app/models/*.rb |
| baseline tables | 99 | `db/migrate/tables/*.rb` |
| curated core (`CORE_V3_RESOURCES`) | 18 | |
| **core classes compiled + emitted** | **16** | 2 dropped: `Activity`, `Priority` — name mismatches (`Priority`≈`IssuePriority<Enumeration`, `Activity`≈`Projects::Activity`/`TimeEntryActivity`); a rename-table (`c4-rename-seed`) input, not a bug |
| Σ fields (attributes+associations) | 303 | over the 16 |
| Σ actions (ActionDef → method stubs) | 323 | rendered as the DO-arm `impl` |
| WorkPackage fields | 40 | 27 baseline cols + assoc; +4 via the W3 replay (→ still narrow, <64) |
| WorkPackage methods | 41 | `new()` + 40 ActionDef stubs |
| **narrow (≤64) emitted** | **16** | all core classes are narrow |
| **wide (>64) deferred** | **0** | no class crosses 64 even with migration-replay (W4 empty by measurement; the `_wide` path stays property-verified, unexercised) |

## Actions by F17 recipe (behaviour arm, from `ruff_openproject::body_triage_probe`)
62-hook behavioural arm classified: **Cascade 46 · Compute 12 · Default 2 · Normalize 1 · Compensate 1**.
**essential_residue = 1** (the single essential `Compensate` core — the only hook not
recipe-recoverable; everything else lowers as an `ActionDef` DO-arm stub). Recipe
coverage = 61/62 ≈ **98.4%** recoverable (`D-ACCIDENTAL-IMPERATIVE` [G] on the OGAR board).

## Classid scheme (V3 NodeGuid/Facet — `8:8:16`)
Each `CompiledClass.facet` (16-byte `ruff_spo_address::Facet`) carries the V3 render classid:
```
u32 = [ domain:8 ][ appid:8 ][ classview:16 ]     canon HIGH / custom LOW
        └ classid  └ app       └ up to 65536 ClassView ids per program
                                  (currently the 0x1000 V3-generation placeholder)
```
Human-readable mnemonic `0x07:01::1000`. OpenProject is one `appid`; the `classview:16`
space is the per-app render/ClassView addressing (the layer after this SHAPE emit).
(NOT the stale `[concept:16][app:16]` of OGAR's older `render_classid`.)

## SurrealQL — DEPRECATED
`ogar_consumer::{emit,render}_surreal_via_ogar` are `#[deprecated]` (operator ruling
2026-07-06); kept compiling, not extended. `op-surreal-ast` still builds. The
non-deprecated path is the V3 OGAR render above.

## Status vs DoD
- ✅ end-to-end reproducible run over the corpus
- ✅ harvest all core models; mint V3 facet classids per class
- ✅ lower the recipe-recoverable behaviour arm into ActionDef method stubs (98.4%)
- ✅ emit COMPILING Rust into the additive `op-generated` landing zone (16 structs)
- ⏳ wire ≥1 real consumer crate (W6, in flight)
- ✅ transpile LEDGER (this file)
- ✅ mark SurrealQL `#[deprecated]`
- ⏸ W3 ruff migration-replay: code+tested, PR ship deferred to the sanctioned MCP path

## Full-graph coverage — EMIT_ALL (2026-07-06, the honest denominator)
The curated-16 above is the *default* path; `EMIT_ALL=1` emits the whole extracted graph. Measured:

| metric | value |
|---|---|
| total compiled (full graph, no filter_to_core) | 945 |
| **emitted structs** | **719** |
| skipped (empty — 0 fields+assocs+actions) | 219 |
| quarantined (uncompilable — real ruff/OGAR bugs, below) | 7 |
| narrow (≤64) / **wide (>64)** | 719 / **0** |
| max field count over ALL 945 | **50** — nothing crosses 64, so the wide leg never fires for this corpus |
| AR graph: from-edges (belongs_to) / to-edges (has_many/has_one/habtm) | **292 / 232** |

**Coverage:** 719 emitted vs the earlier 16 (`16/945` was a misleading ratio — 945 is mostly non-models: 107 real AR classes + ~140 STI + ~428 POROs/services). AR-vs-non-AR can't be split cleanly from what ruff/OGAR capture today (`Model::inherits` is the Odoo slot, unused for Ruby; `is_sti_parent` fires for any non-AR superclass), so the honest figure is **719 of 945 compiled classes with a shape**.

**AR from/to surface:** `crates/op-generated/src/generated/AR_GRAPH.md` — readable per-model `from`/`to` edges (the "where does this come from / go to"). Some targets are `→ ?` where ruff didn't resolve the association's target class (extraction gap).

**7 quarantine findings = real "expand ruff/OGAR" items (whole class skipped, not hand-patched):**
1. `sanitize_ident` maps `?`/`!`/`=` all to `_` → duplicate methods (`CostQuery`, `Message`, `UserPreference`, `Storages::Storage`).
2. Ruby "last `def` wins" harvested as N methods → duplicate defs (`CostQuery::Operator#modify` ×6).
3. Two unrelated `class Methods` merged at ruff's model-graph stage (class-nesting not namespace-prefixed).
4. A trailing Ruby comment leaks into a column name (`t.string :type # …` → field named `type # …`) — `CustomFieldSection`.
