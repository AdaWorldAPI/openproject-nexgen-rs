# Plan — complete OP → op-rs transpile via V3 OGAR (SurrealQL deprecated)

> Goal (operator): complete OP→op-rs transpile using V3 OGAR (not surrealql =
> deprecated). Opus planning/review, Sonnet grindwork, autoresolve, don't stop.
> Pins: OGAR `f8420b9` · ruff `9ef26c1` · lance-graph `d6ab488`. Corpus
> `/home/user/openproject` (`46c1fda2`): **113 app/models, 99 baseline tables,
> 107 AR subclasses**. Curated core `CORE_V3_RESOURCES` = **18**.

## Target pipeline (the non-deprecated V3 path)
```
ruff_ruby_spo::extract_app_with_schema(root,"openproject")
  → filter_to_core (op-nexgen curated 18)
  → ogar_from_ruff::mint::compile_graph_ruby::<OpenProjectPort>
  → Vec<CompiledClass { class, facet, actions }>          // actions = DO-arm, already carried
  → ogar_render_askama::render_class_with_methods(_wide)(class, mask, &actions) → Rust source
```
`ogar_consumer.rs::{emit,render}_surreal_via_ogar` + `lib.rs` SurrealQL emitters
are DEPRECATED — mark `#[deprecated]` (W7), never extend/delete.

## Invariant (operator, reaffirmed 2026-07-06) — ruff + OGAR only, nothing hand-rolled
Every wave: harvest is **ruff** (`ruff_ruby_spo::extract_app_with_schema`), lift/mint
is **OGAR** (`ogar_from_ruff::mint::compile_graph_ruby`), emit is **OGAR**
(`ogar_render_askama::render_class_with_methods`/`_wide`). op-nexgen hand-writes NO
Ruby parser, NO `Class` builder, and NO struct source — it only wires the chain +
curated filters + writes OGAR's output. **FUSE (W1 smoke onward):** every generated
`.rs` carries an `// @generated by ogar_render_askama (OGAR)` provenance header, and
the emit bytes come out of `render_class_with_methods` (a hand-rolled struct string is
a bug). `op-generated` keeps ZERO OGAR/askama deps; those deps live only in the
generator binary under the `ogar-emit` feature.

## Pre-flight audit (verified by Opus planner)
- Every pipeline symbol resolves at current pins EXCEPT: `ogar-render-askama`
  was not a dep of `op-codegen-pipeline`. **FIXED** — added under the
  `ogar-emit` feature (build green). This is the only blocker.
- `CompiledClass` carries `{ class, facet, actions }` (3 fields; `actions:
  Vec<ActionDef>` is the render's 3rd param — no change needed).
- `ActionDef` has no `recipe` field; the F17 recipe verdict is computed in
  `body_triage_probe.rs` from `writes/reads/calls/raises`. "Lower the
  recipe-recoverable arm" = **filter** ActionDefs by the body-triage verdict —
  no OGAR schema change required.
- Baseline-only reader → `WorkPackage` = **40 fields**; the wide leg stays dark
  until the W3 ruff migration-replay wave.

## Landing zone (decided)
**New additive `op-generated` crate** (NOT per-crate `generated/` modules): one
object model in one place; hand-written crates opt in by *depending on* it, so
an isolated crate that nothing depends on yet cannot break a hand-written build.
Emission = a **checked-in generator binary**
`crates/op-codegen-pipeline/src/bin/emit_generated.rs` writing **committed** `.rs`
into `crates/op-generated/src/generated/` (not `build.rs` — reproducible without
the corpus, reviewable in diffs).

## Waves (probe-first; every wave has a pre-registered threshold + drift fuse)

| W | Goal | Local/PR | Grind |
|---|---|---|---|
| **pre** | add `ogar-render-askama` dep — **DONE** | local | done |
| **W0** | harvest-denominator smoke, no emit → ledger. Pass `core_classes==18 & sum_actions>0`; fuse `total_models==113, workpackage_fields==40` | local | Sonnet |
| **W1** | scaffold `op-generated` + `emit_generated.rs` stub. Fuse: workspace green; 0 hand-written crates gain OGAR/askama dep | local | Sonnet (Opus reviews boundary) |
| **W2** | narrow emit (≤64 fields) → committed `.rs`; consume baked masks, fallback FULL (record). Fuse: `TooManyFieldsForMask`→W4, never silent FULL | local | Opus harness once → Sonnet |
| **W3** | **ruff PR**: migration-replay in OP reader → `workpackage_fields>64`. Fuse: `cargo test --workspace` green post lock-bump | ruff PR | Opus |
| **W4** | wide emit (>64) via `_wide`; first real wide-leg run. Fuse: `wide_count≥1`, zero dropped fields | local (gates W3) | Sonnet |
| **W5** | lower recipe-recoverable ActionDefs (shared classifier from `body_triage_probe`) + hand-port the 1 essential Compensate core. Fuse: `essential_residue==1`, ≥98% lowered | local | Opus judgement + Sonnet |
| **W6** | wire ≥1 consumer crate (op-work-packages/op-models) to `op-generated`; delete/thin-reexport the duplicate. Fuse: no dup domain struct | local | Opus + Sonnet |
| **W7** | LEDGER {models/classes/fields/actions-by-recipe/essential-residue} + `#[deprecated]` SurrealQL. Fuse: `op-surreal-ast` still builds | local | Sonnet |

## Sequencing
`pre → W0 → W1` serial. W2⊃W1. **W3 ∥ W2** (W3 only re-runs W0). W4⊃W3. W5⊃W2.
W6⊃W2/W4. W7 last. Sonnet grinders one-crate-each, edit-only, orchestrator
compiles centrally (single `target/`). `cargo test --workspace` gates every
lock bump. All on `claude/beautiful-gates-dJo0u`, floating on main.
